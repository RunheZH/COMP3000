#include <stdio.h>
#include <string.h>

#define MAX_STR 64

void launch();
void printAll();
void addWebsite();
void remWebsite();
void promptForString(char*, char*);
void promptForInt(char*, int*);
void readString(char*);
void readInt(int*);

